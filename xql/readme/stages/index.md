# Stages overview

XQL pipeline stages define query structure and control query flow.

Each stage performs one operation. Stages connect with the pipe (`|`) operator.

* [Browse the full stage index](stages-list)
