{
  "activation_period": "14 Days",
  "attack_tactics": [
    "Execution (TA0002)"
  ],
  "attack_techniques": [
    "Container Administration Command (T1609)",
    "Deploy Container (T1610)"
  ],
  "attackers_goals": "Escape from a container to the host machine and expand the foothold in the network.",
  "deduplication_period": "5 Days",
  "description": "A process from within a Kubernetes Pod communicated with the container runtime daemon using the runtime socket. This may indicate an adversary attempting to escape from the Kubernetes Pod to the host.",
  "detection_modules": "",
  "detector_tags": "Kubernetes - AGENT, Containers",
  "investigative_actions": "Change the container socket configuration. Check if the default Docker daemon binding to TCP changed. If so, non-root users may gain access to the container.",
  "name": "Suspicious container runtime connection from within a Kubernetes Pod",
  "required_data": [
    "XDR Agent"
  ],
  "severity": "Informational",
  "test_period": "N/A (single event)",
  "variations": [
    {
      "name": "Suspicious container runtime connection from within a Kubernetes Pod using the curl client",
      "severity": "Low",
      "attack_tactics": [
        "Execution (TA0002)"
      ],
      "attack_techniques": [
        "Container Administration Command (T1609)",
        "Deploy Container (T1610)"
      ],
      "description": "A process from within a Kubernetes Pod communicated with the container runtime daemon using the runtime socket. This may indicate an adversary attempting to escape from the Kubernetes Pod to the host.",
      "attackers_goals": "Escape from a container to the host machine and expand the foothold in the network.",
      "investigative_actions": "Change the container socket configuration. Check if the default Docker daemon binding to TCP changed. If so, non-root users may gain access to the container."
    },
    {
      "name": "Suspicious container runtime connection from within a Kubernetes Pod using the docker client",
      "severity": "Medium",
      "attack_tactics": [
        "Execution (TA0002)"
      ],
      "attack_techniques": [
        "Container Administration Command (T1609)",
        "Deploy Container (T1610)"
      ],
      "description": "A process from within a Kubernetes Pod communicated with the container runtime daemon using the runtime socket. This may indicate an adversary attempting to escape from the Kubernetes Pod to the host.",
      "attackers_goals": "Escape from a container to the host machine and expand the foothold in the network.",
      "investigative_actions": "Change the container socket configuration. Check if the default Docker daemon binding to TCP changed. If so, non-root users may gain access to the container."
    }
  ]
}
