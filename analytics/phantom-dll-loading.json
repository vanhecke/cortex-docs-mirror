{
  "activation_period": "14 Days",
  "attack_tactics": [
    "Persistence (TA0003)"
  ],
  "attack_techniques": [
    "Hijack Execution Flow: DLL (T1574.001)"
  ],
  "attackers_goals": "An attacker is attempting to load untrusted code into trusted contexts to avoid detection, persist or escalate privileges.",
  "deduplication_period": "1 Day",
  "description": "An attacker might leverage existing processes missing module loads to load malicious code into trusted processes.",
  "detection_modules": "",
  "detector_tags": "DLL Hijacking Analytics",
  "investigative_actions": "Investigate the loaded module and verify if it is malicious.",
  "name": "Phantom DLL Loading",
  "required_data": [
    "XDR Agent"
  ],
  "severity": "Medium",
  "test_period": "N/A (single event)",
  "variations": [
    {
      "name": "Phantom DLL Loading was used to load a DLL into a process after it was extracted from an internet-downloaded archive",
      "severity": "High",
      "attack_tactics": [
        "Persistence (TA0003)"
      ],
      "attack_techniques": [
        "Hijack Execution Flow: DLL (T1574.001)"
      ],
      "description": "An attacker might leverage existing processes missing module loads to load malicious code into trusted processes.",
      "attackers_goals": "An attacker is attempting to load untrusted code into trusted contexts to avoid detection, persist or escalate privileges.",
      "investigative_actions": "Investigate the loaded module and verify if it is malicious."
    },
    {
      "name": "Phantom DLL Loading was used to load a DLL into a process after it was downloaded from an uncommon source",
      "severity": "High",
      "attack_tactics": [
        "Persistence (TA0003)"
      ],
      "attack_techniques": [
        "Hijack Execution Flow: DLL (T1574.001)"
      ],
      "description": "An attacker might leverage existing processes missing module loads to load malicious code into trusted processes.",
      "attackers_goals": "An attacker is attempting to load untrusted code into trusted contexts to avoid detection, persist or escalate privileges.",
      "investigative_actions": "Investigate the loaded module and verify if it is malicious."
    }
  ]
}
